name: pagecairn

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      APP_DATA_DIR: /data
      NODE_ENV: production
    volumes:
      - pagecairn-data:/data
    restart: unless-stopped

volumes:
  pagecairn-data:
